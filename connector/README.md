# Connector

Reserved for Python connector code.

Planned first iteration:

- read lesson JSON from a file;
- read `DJANGO_IMPORT_URL` and `INTERNAL_IMPORT_TOKEN` from environment variables;
- send the lesson JSON to the Django backend import endpoint;
- later expose the same logic through an MCP-style wrapper.

No connector code has been created yet.
