#!/usr/bin/env bash
set -Eeuo pipefail

if [[ "${SSH_ORIGINAL_COMMAND:-}" != "deploy" ]]; then
  echo "This SSH key only accepts: ssh USER@HOST deploy" >&2
  exit 1
fi

exec sudo -n /usr/local/sbin/murrlex-deploy
