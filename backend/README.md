# Backend

Reserved for the Django backend.

Planned first iteration:

- accept lesson JSON;
- save lessons, cards, and raw JSON;
- expose a small Lab UI for manual inspection;
- provide an internal import API protected by `INTERNAL_IMPORT_TOKEN`.

No Django project has been created yet.
